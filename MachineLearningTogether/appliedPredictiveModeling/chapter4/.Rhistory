library(caret)
data(oil)
View(fattyAcids)
?data
str(oilType)
length(oilType)
sum(fattyAcids[1,])
sum(fattyAcids[2,])
sum(fattyAcids[3,])
sum(fattyAcids[4,])
table(oilType)
hist(oilType)
so<-sample(oilType)
trainOil<-oilType[so]
table(trainOil)
so<-sample(oilType)
trainOil<-oilType[so]
table(trainOil)
so<-sample(oilType)
trainOil<-oilType[so]
table(trainOil)
set.seed(23)
so<-sample(oilType)
trainOil<-oilType[so]
table(trainOil)
set.seed(230)
so<-sample(oilType)
trainOil<-oilType[so]
table(trainOil)
set.seed(19)
so<-sample(oilType)
trainOil<-oilType[so]
table(trainOil)
table(oilType)
trainOil
?sample
so<-sample(oilType,60)
trainOil<-oilType[so]
table(trainOil)
oilType
table(oilType)
so<-sample(oilType,60, replace = FALSE)
trainOil<-oilType[so]
table(trainOil)
so<-sample(oilType,60, replace = TRUE)
trainOil<-oilType[so]
table(trainOil)
trainOil
sample(1:3)
so<-sample(60)
so
trainOil<-oilType[so]
table(trainOil)
library(caret)
data(oil)
str(oilType)
table(oilType)
so<-sample(60)
trainOil<-oilType[so]
table(trainOil)
so<-sample(60)
trainOil<-oilType[so]
table(trainOil)
set.seed(21)
so<-sample(60)
trainOil<-oilType[so]
table(trainOil)
set.seed(102)
so<-sample(60)
trainOil<-oilType[so]
table(trainOil)
so<-sample(60)
trainOil<-oilType[so]
table(trainOil)
set.seed(1)
so<-sample(60)
trainOil<-oilType[so]
table(trainOil)
so<-sample(oilType,60)
so
trainOil<-sample(oilType,60)
table(trainOil)
trainOil<-sample(oilType,60)
table(trainOil)
trainOil<-sample(oilType,60)
table(trainOil)
table(oilType)
trainOil<-createDataPartition(oilType, 60, list = FALSE)
View(trainOil)
60/96
trainOil<-createDataPartition(oilType, p = 0.625, list = FALSE)
trainOil<-sample(oilType,60)
length(trainOil)
trainOil2<-createDataPartition(oilType, p = 0.625, list = FALSE)
View(trainOil2)
library(caret)
data(oil)
str(oilType)
table(oilType)
trainOil<-sample(oilType,60)
table(trainOil)
soStr<-createDataPartition(oilType, p = 0.625, list = FALSE)
trainOil2<-oilType[soStr]
table(trainOil2)
library(caret)
data(oil)
str(oilType)
table(oilType)
trainOil<-sample(oilType,60)
table(trainOil)
soStr<-createDataPartition(oilType, p = 0.625, list = FALSE)
trainOil2<-oilType[soStr]
table(trainOil2)
24/37
17/26
2/3
7/10
5/7
binom.test(18,30)
binom.test(26,30)
binom.test(16,20)
16/20
binom.test(16,30)
binom.test(16,40)
binom.test(16,50)
